"""Kickstart helpers — Python ports of ``kickstart/*.sh``.

Two CLIs live here:

* ``python3 -m fedora_setup.kickstart.download_iso`` — interactive Fedora
  ISO browser & SHA-256 verifier (port of ``download-iso.sh``).
* ``python3 -m fedora_setup.kickstart.prepare_ks`` — interactive
  kickstart file generator (port of ``prepare-ks.sh``).
"""
